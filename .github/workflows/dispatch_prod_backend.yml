name: Prod Backend Dispatch
on:
  push:
    branches:
      - 'release-backend'
jobs:
    deploy_backend:
        name: Deploy Backend API
        uses: ./.github/workflow_jobs/deploy_backend.yml
        secrets: inherit