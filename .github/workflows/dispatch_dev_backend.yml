name: Dev Backend Dispatch
on:
  push:
    branches:
      - 'backend/**'
jobs:
    deploy_backend:
        name: Deploy Backend API
        uses: ./.github/workflow_jobs/deploy_backend.yml
        secrets: inherit