name: Test iOS App

on:
  workflow_call:

jobs:
  # ===============================
  # 1️⃣ Swift Package (Library) テスト
  # ===============================
  test-iosapp-library:
    name: Test Swift Package
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.0"

      - name: Cache SPM
        uses: actions/cache@v3
        with:
          path: ~/Library/Caches/org.swift.swiftpm
          key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: ${{ runner.os }}-spm-

      - name: Cache DerivedData
        uses: actions/cache@v3
        with:
          path: Library/.derivedData
          key: ${{ runner.os }}-derived-${{ hashFiles('App/MaTool.xcodeproj/project.pbxproj', '**/*.swift', '**/*.h') }}
          restore-keys: |
            ${{ runner.os }}-derived-

      - name: Build&Test
        run: |
          mkdir -p build/reports
          xcodebuild \
            -project App/MaTool.xcodeproj \
            -scheme iOSApp \
            -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.1' \
            -configuration Debug \
            -derivedDataPath Library/.derivedData \
            -enableCodeCoverage YES \
            -skipMacroValidation \
            CODE_SIGNING_ALLOWED=NO \
            ENABLE_SWIFT_MACROS=YES \
            test \
            | tee build/reports/test.log

      - name: Upload Test Results
        id: upload-tests
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-test-results
          path: Library/.derivedData/Logs/Test/*.xcresult

      - name: Print Failed Tests
        if: always()
        run: |
          xcresult_path=$(ls Library/.derivedData/Logs/Test/*.xcresult | head -n 1)
          echo "===== Failed Tests ====="
          # Legacy モードで全テスト出力
          xcrun xcresulttool get object --path "$xcresult_path" --format text --legacy



  # ===============================
  # 2️⃣ iOS App ビルド（構文・設定確認）
  # ===============================
  # build-app:
  #   name: Build iOS App
  #   runs-on: macos-26

  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4

  #     - name: Setup Xcode
  #       uses: maxim-lobanov/setup-xcode@v1
  #       with:
  #         xcode-version: "26.0"

  #     - name: Cache SPM
  #       uses: actions/cache@v3
  #       if: always()
  #       with:
  #         path: ~/Library/Caches/org.swift.swiftpm
  #         key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
  #         restore-keys: ${{ runner.os }}-spm-

  #     - name: Cache DerivedData
  #       uses: actions/cache@v3
  #       if: always()
  #       with:
  #         path: .derivedData
  #         key: ${{ runner.os }}-derived-${{ hashFiles('App/MaTool.xcodeproj/project.pbxproj', '**/*.swift', '**/*.h') }}
  #         restore-keys: |
  #           ${{ runner.os }}-derived-

  #     - name: Build iOS App
  #       run: |
  #         xcodebuild build \
  #           -project App/MaTool.xcodeproj \
  #           -scheme MaTool \
  #           -sdk iphoneos \
  #           -configuration Release \
  #           -destination 'generic/platform=iOS' \
  #           -derivedDataPath .derivedData \
  #           -skipMacroValidation \
  #           CODE_SIGNING_ALLOWED=NO \
  #           ENABLE_SWIFT_MACROS=YES

  # # ===============================
  # # 3️⃣ まとめ（オプション）
  # # ===============================
  # summary:
  #   name: CI Summary
  #   runs-on: ubuntu-latest
  #   needs: [test-iosapp-library, build-app]
  #   if: always()
  #   steps:
  #     - name: Report
  #       run: echo "✅ Library tests and App build finished."
